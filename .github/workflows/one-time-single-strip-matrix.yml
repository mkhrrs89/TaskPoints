name: One-time single-strip Matrix animation

on:
  pull_request_target:
    types: [opened, synchronize, reopened]

permissions:
  contents: write

jobs:
  patch:
    if: >-
      github.event.pull_request.head.ref == 'automation/single-strip-matrix' &&
      github.event.pull_request.head.repo.full_name == github.repository &&
      github.event.pull_request.base.ref == 'main'
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: automation/single-strip-matrix
          token: ${{ secrets.GITHUB_TOKEN }}
          fetch-depth: 0
      - run: python3 .github/scripts/one_time_single_strip_matrix.py
      - run: |
          set -euo pipefail
          git diff --check
          mapfile -t changed < <(git diff --name-only | sort)
          for file in "${changed[@]}"; do
            case "$file" in
              index.html|.github/scripts/one_time_single_strip_matrix.py|.github/automation-triggers/single-strip-matrix.txt) ;;
              *) echo "Unexpected changed file: $file"; exit 1 ;;
            esac
          done
          printf '%s\n' "${changed[@]}" | grep -qx index.html
          git config user.name "TaskPoints Automation"
          git config user.email "actions@users.noreply.github.com"
          git add -A -- index.html .github/scripts/one_time_single_strip_matrix.py .github/automation-triggers/single-strip-matrix.txt
          git commit -m "Use one smooth Matrix film strip"
          git push origin HEAD:automation/single-strip-matrix
